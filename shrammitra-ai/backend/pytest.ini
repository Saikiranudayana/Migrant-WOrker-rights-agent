[pytest]
asyncio_mode = auto
testpaths = tests
addopts = --tb=short -q

markers =
    unit: Unit tests (no external dependencies)
    integration: Integration tests (needs running services)
    security: Security-focused tests
    rag: RAG pipeline tests
